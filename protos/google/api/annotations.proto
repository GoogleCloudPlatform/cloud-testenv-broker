syntax = "proto3";

// FIXME(gbin): Made up proto so the thing compiles from the same source proto.

package google.api;

import "google/protobuf/descriptor.proto";
import "google/api/http.proto";

extend google.protobuf.MethodOptions {
  // See `HttpRule`.
  HttpRule http = 72295728;
}

